# Copilot Instructions 업데이트 완료

## 추가된 내용: Natural Language Command Detection

### 위치
`copilot-instructions.md` 파일의 상단, Overview 바로 다음

### 주요 기능

#### 1. **명시적 명령어** (최우선)
- `/web`, `/pptx`, `/contents`, `/style`, `/full` 등
- 바로 실행

#### 2. **자연어 의도 감지** (자동 라우팅)

**웹 개발 키워드:**
- "웹사이트", "사이트", "HTML", "CSS", "코드 생성"
- URL 포함 (https://, http://)
- → `/web` 파이프라인 자동 실행

**프레젠테이션 키워드:**
- "PPT", "PPTX", "슬라이드", "발표", "피칭"
- "투자", "제안서"
- → `/pptx` 파이프라인 자동 실행

**분석만:**
- "분석만", "구조만", "디자인만"
- → `/contents` 또는 `/style`

#### 3. **애매한 입력** (역질문)

URL만 있고 의도가 불명확할 때:
```
이 사이트를 어떻게 분석할까요?

1. 🌐 /web - 웹사이트 전체 분석 → HTML/CSS/JS 코드 생성
2. 📊 /pptx - 프레젠테이션용 분석 → 슬라이드 스펙 생성
3. 📝 /contents - 콘텐츠 구조만 분석
4. 🎨 /style - 비주얼/스타일만 분석
5. 🔄 /full - 전체 파이프라인 실행

어떤 방식으로 진행할까요?
```

#### 4. **의도 파악 불가**

명확한 요청이 없을 때 상세 질문

### 예시

| 사용자 입력 | 감지된 의도 | 액션 |
|------------|------------|------|
| "https://example.com 분석해줘" | 애매함 | 옵션 제시 |
| "이 사이트 HTML로 만들어줘" | 웹 개발 | `/web` 자동 실행 |
| "PPT 만들어줘" | 프레젠테이션 | `/pptx` 자동 실행 |
| "/web https://example.com" | 명시적 | `/web` 실행 |
| "이 웹페이지 색상 분석" | 스타일 분석 | `/style` 자동 실행 |

## 이제 동작 방식

1. **사용자가 URL만 제공** → AI가 역질문하며 옵션 제시
2. **사용자가 "웹사이트 분석" 같은 자연어** → 자동으로 `/web` 실행
3. **사용자가 명령어 사용** → 바로 해당 파이프라인 실행

## 다음 테스트

이제 다시 시도해보세요:
```
https://www.getnauta.com/ 여기 분석해줘
```

AI가 이렇게 응답해야 합니다:
```
이 사이트를 어떻게 분석할까요?

1. 🌐 /web - 웹사이트 전체 분석...
2. 📊 /pptx - 프레젠테이션용 분석...
...
```
